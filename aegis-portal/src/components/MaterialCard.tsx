import { Card } from './ui/Card'

export default function MaterialCard() {
  return (
    <Card className="material-card">
      <p>Material benchmark data is no longer served by the live DPWH Watchdog API.</p>
    </Card>
  )
}
